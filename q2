#include <stdio.h>

int main() {
    int i;

    for (i = 0; i < 128; i++) {
        printf("%3d: %c   ", i, (char)i);

        // Imprimir 10 caracteres por linha
        if ((i + 1) % 10 == 0)
            printf("\n");
    }

    return 0;
}
